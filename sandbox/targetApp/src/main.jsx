import React from "react";
import ReactDOM from "react-dom/client";
import TargetApp from "./TargetApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TargetApp />
  </React.StrictMode>
);
